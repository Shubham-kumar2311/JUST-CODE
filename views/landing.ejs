<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <title>JUST CODE</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    html, body {
      height: 100%;
      width: 100%;
      scroll-behavior: smooth;
    }

    /* Main Hero Section */
    .hero-section {
      background: linear-gradient(135deg, #1a1a1a 0%, #2c2c2c 100%);
      color: white;
      min-height: 85vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 2rem 1rem;
      position: relative;
      overflow: hidden;
    }

    .hero-section h1 {
      font-size: 3.5rem;
      font-weight: 800;
      margin-bottom: 1rem;
      animation: fadeInDown 1s ease-out;
    }

    .hero-section p {
      font-size: 1.25rem;
      opacity: 0.8;
      margin-bottom: 2rem;
      animation: fadeInUp 1s ease-out 0.3s;
      animation-fill-mode: both;
    }

    .hero-section .btn-primary {
      background-color: #ff4d4d;
      border: none;
      padding: 0.75rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 50px;
      transition: transform 0.3s ease, background-color 0.3s ease;
      animation: fadeInUp 1s ease-out 0.6s;
      animation-fill-mode: both;
    }

    .hero-section .btn-primary:hover {
      background-color: #e03e3e;
      transform: translateY(-3px);
    }

    /* Footer */
    footer {
      background: #1a1a1a;
      color: white;
      text-align: center;
      padding: 1.5rem;
      font-size: 0.9rem;
    }

    /* Animations */
    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .hero-section h1 {
        font-size: 2.5rem;
      }

      .hero-section p {
        font-size: 1rem;
      }
    }

    @media (max-width: 576px) {
      .hero-section h1 {
        font-size: 2rem;
      }

      .hero-section .btn-primary {
        padding: 0.5rem 1.5rem;
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/nav') %>

  <!-- Hero Section -->
  <section class="hero-section">
    <h1>Master Coding Challenges</h1>
    <p>Level up your skills, ace interviews, and compete globally with CodeMaster.</p>
    <a href="/problems" class="btn btn-primary">Start Coding Now</a>
  </section>

  <!-- Footer -->
  <footer>
    <p>© <%= new Date().getFullYear() %> JustCode. All rights reserved.</p>
  </footer>

  <%- include('partials/scripts') %>
</body>
</html>